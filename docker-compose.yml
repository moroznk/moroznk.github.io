services:
  site: &base-service
    build: .
    ports:
      - "4000:4000"
    environment:
      JEKYLL_UID: 1001
      JEKYLL_GID: 1001
    volumes:
      - ./site:/srv/jekyll/site

  builder:
    <<: *base-service
    command: jekyll build && ls _site
